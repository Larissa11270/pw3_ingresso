# pw3-ingresso
Kaio de Oliveira, Larissa Amaral

CREATE TABLE TBL_SESSAO (
    ID_SESSAO BIGINT PRIMARY KEY IDENTITY,
    PRECO FLOAT,
    SALA VARCHAR,
    HORARIO TIME,
    DATA DATE,
    FK_ID_FILME BIGINT,
    FOREIGN KEY(FK_ID_FILME) REFERENCES TBL_FILME(ID_FILME)
);
